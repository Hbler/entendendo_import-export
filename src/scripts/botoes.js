// export simples, declarado junto com a declaração da variável
export const um = () => {
  const botao = document.createElement("button");

  botao.className = "botao-um";
  botao.innerText = "Botão Um";

  return botao;
};

// export simples, declarado junto com a declaração da variável
export const dois = () => {
  const botao = document.createElement("button");

  botao.className = "botao-dois";
  botao.innerText = "Botão Dois";

  return botao;
};

// export simples, declarado junto com a declaração da variável
export const tres = () => {
  const botao = document.createElement("button");

  botao.className = "botao-tres";
  botao.innerText = "Botão Três";

  return botao;
};
